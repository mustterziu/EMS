<!-- Modal -->
<div class="modal fade" id="deleteModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Kujdes!</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                Jeni sigurt qe deshironi ta perfundoni kete veprim?!
                <form method="post" id="deleteEmp" asp-controller="Employee" asp-action="DeleteEmployee">
                    <input type="hidden" id="id" name="id" value="">
                    <input type="hidden" id="returnUrl" name="returnUrl"/>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Anulo</button>
                <button type="submit" form="deleteEmp" class="btn btn-primary btn-danger">Fshij</button>
            </div>
        </div>
    </div>
</div>